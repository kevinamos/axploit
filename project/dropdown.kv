#:import Button kivy.uix.button.Button
<MainView>:
    ComboEdit:
        size_hint: .5, .5
        pos_hint: {'center':(.5, .5)}
        options:
            [Button(text = str(x),size_hint_y=None,height=30)for x in range(10)]



